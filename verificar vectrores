 #!/bin/bash
function vectores()
{ 
   login= $(wget $1 | grep "login")
   coment= $(wget $1 | grep"comment")
   filter= $(wget $1 | grep"filter") 
   search= $(wget $1 | grep"search")
   category= $(wget $1 | grep"comment")
   if [ -e $login ]
      diccionario=true
   if [ -e $filter ] || [-e $category ]
      sql=true
   if [ -e $comment ] || [ -e $search ] 
      xss=true
}
